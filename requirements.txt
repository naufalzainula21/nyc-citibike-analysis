# Urban Data Science Portfolio - Python Requirements
# Install all packages with: pip install -r requirements.txt

# Core Data Science
pandas>=1.5.0
numpy>=1.23.0
scipy>=1.9.0
scikit-learn>=1.1.0

# Visualization
matplotlib>=3.6.0
seaborn>=0.12.0

# Geospatial Analysis
geopandas>=0.12.0
shapely>=2.0.0
folium>=0.14.0
pyproj>=3.4.0

# Spatial Statistics (Optional - for advanced spatial analysis)
libpysal>=4.7.0
esda>=2.4.0

# Computer Vision (for Project 3)
torch>=2.0.0
torchvision>=0.15.0
opencv-python>=4.7.0
Pillow>=9.3.0

# Jupyter
jupyter>=1.0.0
ipykernel>=6.20.0
notebook>=6.5.0

# Utilities
requests>=2.28.0
python-dotenv>=0.21.0  # For API keys management

# Data Loading
openpyxl>=3.0.10  # For Excel files
xlrd>=2.0.1  # For older Excel formats

# Optional but recommended
tqdm>=4.64.0  # Progress bars
jupyter-contrib-nbextensions>=0.7.0  # Jupyter extensions


# ===== INSTALLATION INSTRUCTIONS =====
#
# 1. Create virtual environment (recommended):
#    python -m venv venv
#    source venv/bin/activate  # On Windows: venv\Scripts\activate
#
# 2. Install all packages:
#    pip install -r requirements.txt
#
# 3. For GPU support with PyTorch (optional):
#    Visit https://pytorch.org/get-started/locally/
#    and install the appropriate version for your system
#
# ===== PROJECT-SPECIFIC REQUIREMENTS =====
#
# Project 1 (Mobility Analysis):
#   - pandas, numpy, geopandas, folium, matplotlib, seaborn, scipy, scikit-learn
#
# Project 2 (Housing Regression):
#   - pandas, numpy, geopandas, shapely, matplotlib, seaborn, scipy, scikit-learn
#   - Optional: libpysal, esda (for spatial statistics)
#
# Project 3 (Computer Vision):
#   - torch, torchvision, opencv-python, Pillow, numpy, matplotlib
#
# ===== TROUBLESHOOTING =====
#
# If geopandas installation fails:
#   1. Install GDAL first (system-dependent)
#   2. Or use conda: conda install geopandas
#
# If PyTorch installation is slow:
#   Install CPU-only version:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#
# If on Windows and facing issues:
#   Consider using Anaconda distribution which has pre-built binaries
#
